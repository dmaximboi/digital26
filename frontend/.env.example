# Frontend (Vite). Copy to frontend/.env — never commit secrets.

VITE_API_URL=
VITE_NEON_AUTH_URL=
# Secret path only (no slashes). Example shape: console-xxxxxxxx
# Do NOT commit the real value. Required for admin UI routes to exist.
VITE_ADMIN_PATH=
VITE_PUBLIC_SITE_URL=https://digital26.online
